import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'NavKiran_Sathi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
